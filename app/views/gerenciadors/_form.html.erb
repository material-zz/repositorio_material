<%= form_for(@gerenciador) do |f| %>
  <% if @gerenciador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gerenciador.errors.count, "error") %> prohibited this gerenciador from being saved:</h2>

      <ul>
      <% @gerenciador.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :login %><br />
    <%= f.text_field :login %>
  </div>
  <div class="field">
    <%= f.label :senha %><br />
    <%= f.text_field :senha %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
